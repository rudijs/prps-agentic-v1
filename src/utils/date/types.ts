
/**
 * @interface TimeOptions
 * @description Options for formatting time.
 */
export interface TimeOptions {
  format?: string;
  timezone?: string;
}

/**
 * @interface DateOptions
 * @description Options for formatting dates.
 */
export interface DateOptions {
  format?: string;
  locale?: string;
}
